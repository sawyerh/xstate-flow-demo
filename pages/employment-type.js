export default function Page() {
  return (
    <>
      <h1>Employed or unemployed?</h1>
      <p>
        Set <code>"employement-type"</code> in the form state to either{" "}
        <code>"employed"</code> or <code>"unemployed"</code> to observe the
        conditional routing.
      </p>
    </>
  );
}
